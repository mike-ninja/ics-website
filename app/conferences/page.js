import ConferencesCards from "@/components/ConferencesCards";

// /post

const page = () => {
  return (
    <div className="container">
      <ConferencesCards />
    </div>
  );
};

export default page;
